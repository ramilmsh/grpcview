load("@aspect_bazel_lib//lib:copy_to_directory.bzl", "copy_to_directory")
load("@aspect_bazel_lib//lib:transitions.bzl", "platform_transition_filegroup")
load("@rules_go//go:def.bzl", "go_binary", "go_cross_binary", "go_library")

RELEASE_PLATFORMS = [
    "darwin_amd64",
    "darwin_arm64",
    "linux_amd64",
    "linux_arm64",
]

platform_transition_filegroup(
    name = "ui_host",
    srcs = ["//ui/src:dist"],
    target_platform = "@platforms//host",
)

copy_to_directory(
    name = "dist",
    srcs = [":ui_host"],
    root_paths = [
        "ui/src/dist",
        "ui/src/theme/dist",
        "ui/src/workers/dist",
    ],
)

go_library(
    name = "cmd_lib",
    srcs = ["main.go"],
    # keep
    embedsrcs = [":dist"],
    importpath = "codeberg.org/ramilmsh/grpcview/service/cmd",
    visibility = ["//visibility:private"],
    deps = [
        "//service",
        "//service/cli",
    ],
)

go_binary(
    name = "grpcview",
    embed = [":cmd_lib"],
    visibility = ["//visibility:public"],
    x_defs = {"codeberg.org/ramilmsh/grpcview/service/cli.version": "{STABLE_VERSION_TAG}"},
)

# Kept: //service/cmd predates the grpcview rename and still gets typed from
# habit and old docs; deleting it would silently break that path.
alias(
    name = "cmd",
    actual = ":grpcview",
    visibility = ["//visibility:public"],
)

[
    go_cross_binary(
        name = "grpcview_" + platform,
        platform = "@rules_go//go/toolchain:" + platform,
        target = ":grpcview",
        visibility = ["//visibility:public"],
    )
    for platform in RELEASE_PLATFORMS
]

filegroup(
    name = "release",
    srcs = ["grpcview_" + platform for platform in RELEASE_PLATFORMS],
    visibility = ["//visibility:public"],
)
