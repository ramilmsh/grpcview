// Intentionally-empty stub. @bufbuild/protoplugin's dist/{cjs,esm}/transpile.js unconditionally
// require()s "@typescript/vfs" at module load, for the same dead transpile() code path
// "typescript" is stubbed for — see that package's index.js.
module.exports = {};
