load("@aspect_rules_ts//ts:defs.bzl", "ts_config")
load("@npm//:defs.bzl", "npm_link_all_packages")

npm_link_all_packages(name = "node_modules")

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    visibility = [":__subpackages__"],
)

# Referenced by every ts_project's transpiler = partial.make(swc, swcrc = "//ui:.swcrc")
# across ui/src/...; see swc-migration-plan.md.
exports_files(
    [".swcrc"],
    visibility = [":__subpackages__"],
)
