load("//ui:defs.bzl", "ts_project")

ts_project(
    name = "vendor",
    srcs = ["dayjs-types.ts"],
    declaration = True,
    no_emit = False,
    preserve_jsx = False,
    resolve_json_module = True,
    tsconfig = "//ui/src/features/scripts:tsconfig",
    visibility = ["//visibility:public"],
)
