load("//ui:defs.bzl", "ts_project")

ts_project(
    name = "vendor",
    srcs = [
        "bufbuild-stubs.ts",
        "ts-stub.ts",
        "ts-vfs-stub.ts",
    ],
    declaration = True,
    no_emit = False,
    preserve_jsx = False,
    resolve_json_module = True,
    tsconfig = "//ui/src/features/workspace:tsconfig",
    visibility = ["//visibility:public"],
    deps = ["//ui:node_modules/monaco-editor"],
)
