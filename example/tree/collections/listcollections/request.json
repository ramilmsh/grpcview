{
  "meta":  {
    "name":  "ListCollections"
  },
  "service":  "grpcview.v1.WorkspaceService",
  "method":  "ListCollections",
  "draftBody":  "export default async (): Promise<RequestMessage> => (\n{\n  // No target: this falls back to the collection's first reflection source,\n  // localhost:10000 — grpcview's own WorkspaceService, reflecting itself.\n  refresh: gv.request.params.refresh ?? true,\n}\n)",
  "middleware":  [],
  "draftMetadataScript":  ""
}
