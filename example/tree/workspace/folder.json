{
  "meta":  {
    "name":  "Workspace"
  },
  "items":  [
    "listcollections",
    "describemethod-json",
    "runscript-params",
    "runscript-generators",
    "runscript-middleware",
    "invoke-chained",
    "streaming"
  ],
  "draftMetadataScript":  "export default async (): Promise<Metadata> => (\n{\n  // Folder metadata: written once here, inherited by every request below.\n  // Everything under this folder talks to grpcview's OWN workspace server on\n  // localhost:10000 — the collection's only reflection source — so no request\n  // carries a target of its own.\n  \"x-demo-suite\": [\"workspace\"],\n  \"x-demo-inherited-from\": [\"Workspace folder\"],\n}\n)"
}
