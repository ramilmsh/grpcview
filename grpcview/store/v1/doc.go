// Package grpcviewstorev1 has no hand-written sources. This file exists only so
// `go build`/`go mod tidy` (run outside Bazel) see this directory as a real
// Go package — Bazel generates the actual sources via go_proto_library.
package grpcviewstorev1
